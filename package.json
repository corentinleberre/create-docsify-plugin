{
  "name": "create-docsify-plugin",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite serve src",
    "build": "vite build --config vite-config.js",
    "test": "vitest"
  },
  "devDependencies": {
    "vite": "^4.0.1",
    "vitest": "^0.25.7"
  }
}
